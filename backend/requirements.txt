fastapi==0.137.2
uvicorn==0.49.0
python-dotenv==1.2.2
python-multipart==0.0.32
ffmpeg-python==0.2.0
# Pinning yt-dlp is a liability: YouTube breaks extraction regularly and only a
# current release keeps working. 2026.6.9 returned HTTP 403 on every download.
# Re-run `pip install -U yt-dlp` before any demo.
yt-dlp>=2026.8.19

faster-whisper
ctranslate2
av

httpx==0.28.1
httptools==0.8.0
uvloop==0.22.1; sys_platform != 'win32'
watchfiles==1.2.0
websockets==16.0

pydantic==2.13.4

pytest==9.1.0
pytest-asyncio==1.4.0
psutil==7.2.2